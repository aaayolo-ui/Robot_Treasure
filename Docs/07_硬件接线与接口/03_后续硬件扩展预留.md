# 后续硬件扩展预留

本文件只记录计划，以下项目均**尚未实机接入和验证**，不得在已验证接线或引脚占用中写成已完成。设计前须先阅读 [02_STM32引脚占用总表.md](02_STM32引脚占用总表.md)。

| 项目 | 当前计划 | 引脚/供电约束 | 状态 |
|---|---|---|---|
| 第二块 NCHD12 | 与第一块共用 PB8=SCL、PB9=SDA；计划地址 `0x21`，地址焊盘 `A2/A1/A0=0/0/1`。 | 尚未接线；确认地址、供电和总线后才能上电验证。 | 仅后续计划 |
| AI-1811 ToF | 已购 1 个，先做电脑串口测试；主动上报 UART。 | USART3 的 PB10/PB11 已用于调试；最终 STM32 UART、引脚与多模块接入方式未定，不分配假引脚。 | 仅后续计划 |
| 普通红外模块 | 备用/对比测试。 | GPIO 尚未分配。 | 仅后续计划 |
| 蜂鸣器 | 声音提示候选。 | GPIO 与驱动方式尚未分配。 | 仅后续计划 |
| 外部状态 LED | 状态提示候选。 | GPIO 与限流/电平方案尚未分配。 | 仅后续计划 |
| 电池电压 ADC | 电量采样候选。 | ADC 引脚、分压网络和参考电压尚未确认。 | 仅后续计划 |
| 外部 DC-DC 最终供电 | 整车可能由电池控制支路经 DC-DC 给 STM32 和传感器供电。 | 切换前用万用表确认输出电压与极性；DC-DC 5V 不得与 MD240A `VOUT 5V` 并联。 | 仅后续计划 |
| 板载 1.44 寸 TFT | 板载但当前工程未启用。 | 缺少可独立确认的完整引脚表；获得原理图并验证前不分配引脚。 | 仅后续计划 |

## 扩展实施门槛

- 先检查当前占用和开发板实际引出，不能预填不存在的 GPIO、ADC 或 UART 分配。
- ToF 的多个主动上报 TX 不能直接并联到同一 STM32 UART_RX；须在实物测试后确定串口、切换或外部复用方案。
- 外部 DC-DC 切换前确认电压和极性；切换后保持单一 STM32 供电源与全系统共地。
- 每项完成实体接线与实机测试后，补充实测条件和结论，再迁入 `01` 与 `02`。
